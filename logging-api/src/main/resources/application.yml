server:
  port: 8090

spring:
  application:
    name: logging-api

  datasource:
    url: jdbc:mysql://localhost:3306/observability
    username: npst
    password: npst123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true